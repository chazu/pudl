package {{.PackageName}}

// {{.SchemaName}} defines the schema for {{.SuggestedName}} data
#{{.SchemaName}}: {
	// PUDL metadata for schema management
	_pudl: {
		schema_type: "base" // Valid: "base", "collection", "policy", "catchall"
		resource_type: "{{.PackageName}}.{{.SchemaNameLower}}" // Format: <package>.<type> - identifies this resource type
		cascade_priority: 100 // 0-1000, higher = more specific (catchall=0, base=100, policy=200+)
		cascade_fallback: ["core.#Item"] // Schemas to try if this doesn't match
		identity_fields: {{.IdentityFields}}
		tracked_fields: {{.TrackedFields}}
		compliance_level: "strict" // Valid: "strict", "warn", "permissive"
	}

	// Field definitions (customize as needed)
{{.FieldDefinitions}}
}

// Example data that matches this schema:
{{.ExampleJSON}}
